# Generated from ~/.config/hypr/UserConfigs/Hypridle.lua -- do not edit by hand.

$iDIR="$HOME/.config/swaync/images/ja.png"

general {
    lock_cmd = pidof hyprlock || hyprlock
    before_sleep_cmd = loginctl lock-session
    after_sleep_cmd = hyprctl dispatch dpms on
    ignore_dbus_inhibit = false
}

# Idle warning (295.0 min)
listener {
    timeout = 17700
    on-timeout = notify-send -i $iDIR ' You are idle!'
    on-resume = notify-send -i $iDIR ' Oh! you are Back' ' Hello !!!'
}

# Screenlock (300.0 min)
listener {
    timeout = 18000
    on-timeout = loginctl lock-session
}
